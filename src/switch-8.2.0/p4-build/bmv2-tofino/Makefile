# Makefile.in generated by automake 1.14.1 from Makefile.am.
# bmv2-tofino/Makefile.  Generated from Makefile.in by configure.

# Copyright (C) 1994-2013 Free Software Foundation, Inc.

# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy and/or distribute it,
# with or without modifications, as long as this notice is preserved.

# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY, to the extent permitted by law; without
# even the implied warranty of MERCHANTABILITY or FITNESS FOR A
# PARTICULAR PURPOSE.






am__is_gnu_make = test -n '$(MAKEFILE_LIST)' && test -n '$(MAKELEVEL)'
am__make_running_with_option = \
  case $${target_option-} in \
      ?) ;; \
      *) echo "am__make_running_with_option: internal error: invalid" \
              "target option '$${target_option-}' specified" >&2; \
         exit 1;; \
  esac; \
  has_opt=no; \
  sane_makeflags=$$MAKEFLAGS; \
  if $(am__is_gnu_make); then \
    sane_makeflags=$$MFLAGS; \
  else \
    case $$MAKEFLAGS in \
      *\\[\ \	]*) \
        bs=\\; \
        sane_makeflags=`printf '%s\n' "$$MAKEFLAGS" \
          | sed "s/$$bs$$bs[$$bs $$bs	]*//g"`;; \
    esac; \
  fi; \
  skip_next=no; \
  strip_trailopt () \
  { \
    flg=`printf '%s\n' "$$flg" | sed "s/$$1.*$$//"`; \
  }; \
  for flg in $$sane_makeflags; do \
    test $$skip_next = yes && { skip_next=no; continue; }; \
    case $$flg in \
      *=*|--*) continue;; \
        -*I) strip_trailopt 'I'; skip_next=yes;; \
      -*I?*) strip_trailopt 'I';; \
        -*O) strip_trailopt 'O'; skip_next=yes;; \
      -*O?*) strip_trailopt 'O';; \
        -*l) strip_trailopt 'l'; skip_next=yes;; \
      -*l?*) strip_trailopt 'l';; \
      -[dEDm]) skip_next=yes;; \
      -[JT]) skip_next=yes;; \
    esac; \
    case $$flg in \
      *$$target_option*) has_opt=yes; break;; \
    esac; \
  done; \
  test $$has_opt = yes
am__make_dryrun = (target_option=n; $(am__make_running_with_option))
am__make_keepgoing = (target_option=k; $(am__make_running_with_option))
pkgdatadir = $(datadir)/p4-build
pkgincludedir = $(includedir)/p4-build
pkglibdir = $(libdir)/p4-build
pkglibexecdir = $(libexecdir)/p4-build
am__cd = CDPATH="$${ZSH_VERSION+.}$(PATH_SEPARATOR)" && cd
install_sh_DATA = $(install_sh) -c -m 644
install_sh_PROGRAM = $(install_sh) -c
install_sh_SCRIPT = $(install_sh) -c
INSTALL_HEADER = $(INSTALL_DATA)
transform = $(program_transform_name)
NORMAL_INSTALL = :
PRE_INSTALL = :
POST_INSTALL = :
NORMAL_UNINSTALL = :
PRE_UNINSTALL = :
POST_UNINSTALL = :
build_triplet = x86_64-unknown-linux-gnu
host_triplet = x86_64-unknown-linux-gnu
am__append_1 = -DHOST_BYTE_ORDER_CALLER
#am__append_2 = $(P4_PI_NATIVE_JSON_OUTPUT)
#am__append_3 = $(P4_PI_NATIVE_JSON_OUTPUT)
subdir = bmv2-tofino
DIST_COMMON = $(srcdir)/Makefile.in $(srcdir)/Makefile.am \
	$(top_srcdir)/depcomp $(extrapython_PYTHON) $(p4python_PYTHON) \
	$(top_srcdir)/py-compile $(pdinclude_HEADERS) \
	$(pdthriftsrcinclude_HEADERS)
ACLOCAL_M4 = $(top_srcdir)/aclocal.m4
am__aclocal_m4_deps = $(top_srcdir)/m4/ab_find_header_path.m4 \
	$(top_srcdir)/m4/ac_p4c.m4 \
	$(top_srcdir)/m4/adl_recursive_eval.m4 \
	$(top_srcdir)/m4/ax_cxx_compile_stdcxx_11.m4 \
	$(top_srcdir)/m4/ax_restore_flags.m4 \
	$(top_srcdir)/m4/ax_save_flags.m4 $(top_srcdir)/m4/libtool.m4 \
	$(top_srcdir)/m4/ltoptions.m4 $(top_srcdir)/m4/ltsugar.m4 \
	$(top_srcdir)/m4/ltversion.m4 $(top_srcdir)/m4/lt~obsolete.m4 \
	$(top_srcdir)/configure.ac
am__configure_deps = $(am__aclocal_m4_deps) $(CONFIGURE_DEPENDENCIES) \
	$(ACLOCAL_M4)
mkinstalldirs = $(install_sh) -d
CONFIG_HEADER = $(top_builddir)/config.h
CONFIG_CLEAN_FILES =
CONFIG_CLEAN_VPATH_FILES =
am__vpath_adj_setup = srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`;
am__vpath_adj = case $$p in \
    $(srcdir)/*) f=`echo "$$p" | sed "s|^$$srcdirstrip/||"`;; \
    *) f=$$p;; \
  esac;
am__strip_dir = f=`echo $$p | sed -e 's|^.*/||'`;
am__install_max = 40
am__nobase_strip_setup = \
  srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*|]/\\\\&/g'`
am__nobase_strip = \
  for p in $$list; do echo "$$p"; done | sed -e "s|$$srcdirstrip/||"
am__nobase_list = $(am__nobase_strip_setup); \
  for p in $$list; do echo "$$p $$p"; done | \
  sed "s| $$srcdirstrip/| |;"' / .*\//!s/ .*/ ./; s,\( .*\)/[^/]*$$,\1,' | \
  $(AWK) 'BEGIN { files["."] = "" } { files[$$2] = files[$$2] " " $$1; \
    if (++n[$$2] == $(am__install_max)) \
      { print $$2, files[$$2]; n[$$2] = 0; files[$$2] = "" } } \
    END { for (dir in files) print dir, files[dir] }'
am__base_list = \
  sed '$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;s/\n/ /g' | \
  sed '$$!N;$$!N;$$!N;$$!N;s/\n/ /g'
am__uninstall_files_from_dir = { \
  test -z "$$files" \
    || { test ! -d "$$dir" && test ! -f "$$dir" && test ! -r "$$dir"; } \
    || { echo " ( cd '$$dir' && rm -f" $$files ")"; \
         $(am__cd) "$$dir" && rm -f $$files; }; \
  }
am__installdirs = "$(DESTDIR)$(pdlibdir)" \
	"$(DESTDIR)$(extrapythondir)" "$(DESTDIR)$(p4pythondir)" \
	"$(DESTDIR)$(pddatadir)" "$(DESTDIR)$(pdincludedir)" \
	"$(DESTDIR)$(pdthriftsrcincludedir)"
LTLIBRARIES = $(pdlib_LTLIBRARIES)
libpd_la_LIBADD =
am__dirstamp = $(am__leading_dot)dirstamp
am__objects_1 = switch/src/pd.lo switch/src/pd_learning.lo \
	switch/src/pd_meters.lo switch/src/pd_counters.lo \
	switch/src/pd_registers.lo switch/src/pd_tables.lo \
	switch/src/pd_ageing.lo
am_libpd_la_OBJECTS = $(am__objects_1)
libpd_la_OBJECTS = $(am_libpd_la_OBJECTS)
AM_V_lt = $(am__v_lt_$(V))
am__v_lt_ = $(am__v_lt_$(AM_DEFAULT_VERBOSITY))
am__v_lt_0 = --silent
am__v_lt_1 = 
libpdthrift_la_LIBADD =
am__objects_2 = pd_thrift_gen/gen-cpp/p4_prefix0.lo \
	pd_thrift_gen/gen-cpp/p4_prefix1.lo \
	pd_thrift_gen/gen-cpp/p4_prefix2.lo \
	pd_thrift_gen/gen-cpp/p4_pd_rpc_constants.lo \
	pd_thrift_gen/gen-cpp/p4_pd_rpc_types.lo
am__objects_3 =
am__objects_4 = $(am__objects_2) $(am__objects_3) $(am__objects_3)
am_libpdthrift_la_OBJECTS = $(am__objects_4) \
	switch/thrift-src/pd_rpc_server.lo
libpdthrift_la_OBJECTS = $(am_libpdthrift_la_OBJECTS)
AM_V_P = $(am__v_P_$(V))
am__v_P_ = $(am__v_P_$(AM_DEFAULT_VERBOSITY))
am__v_P_0 = false
am__v_P_1 = :
AM_V_GEN = $(am__v_GEN_$(V))
am__v_GEN_ = $(am__v_GEN_$(AM_DEFAULT_VERBOSITY))
am__v_GEN_0 = @echo "  GEN     " $@;
am__v_GEN_1 = 
AM_V_at = $(am__v_at_$(V))
am__v_at_ = $(am__v_at_$(AM_DEFAULT_VERBOSITY))
am__v_at_0 = @
am__v_at_1 = 
DEFAULT_INCLUDES = -I. -I$(top_builddir)
depcomp = $(SHELL) $(top_srcdir)/depcomp
am__depfiles_maybe = depfiles
am__mv = mv -f
CXXCOMPILE = $(CXX) $(DEFS) $(DEFAULT_INCLUDES) $(INCLUDES) \
	$(AM_CPPFLAGS) $(CPPFLAGS) $(AM_CXXFLAGS) $(CXXFLAGS)
LTCXXCOMPILE = $(LIBTOOL) $(AM_V_lt) --tag=CXX $(AM_LIBTOOLFLAGS) \
	$(LIBTOOLFLAGS) --mode=compile $(CXX) $(DEFS) \
	$(DEFAULT_INCLUDES) $(INCLUDES) $(AM_CPPFLAGS) $(CPPFLAGS) \
	$(AM_CXXFLAGS) $(CXXFLAGS)
AM_V_CXX = $(am__v_CXX_$(V))
am__v_CXX_ = $(am__v_CXX_$(AM_DEFAULT_VERBOSITY))
am__v_CXX_0 = @echo "  CXX     " $@;
am__v_CXX_1 = 
CXXLD = $(CXX)
CXXLINK = $(LIBTOOL) $(AM_V_lt) --tag=CXX $(AM_LIBTOOLFLAGS) \
	$(LIBTOOLFLAGS) --mode=link $(CXXLD) $(AM_CXXFLAGS) \
	$(CXXFLAGS) $(AM_LDFLAGS) $(LDFLAGS) -o $@
AM_V_CXXLD = $(am__v_CXXLD_$(V))
am__v_CXXLD_ = $(am__v_CXXLD_$(AM_DEFAULT_VERBOSITY))
am__v_CXXLD_0 = @echo "  CXXLD   " $@;
am__v_CXXLD_1 = 
COMPILE = $(CC) $(DEFS) $(DEFAULT_INCLUDES) $(INCLUDES) $(AM_CPPFLAGS) \
	$(CPPFLAGS) $(AM_CFLAGS) $(CFLAGS)
LTCOMPILE = $(LIBTOOL) $(AM_V_lt) --tag=CC $(AM_LIBTOOLFLAGS) \
	$(LIBTOOLFLAGS) --mode=compile $(CC) $(DEFS) \
	$(DEFAULT_INCLUDES) $(INCLUDES) $(AM_CPPFLAGS) $(CPPFLAGS) \
	$(AM_CFLAGS) $(CFLAGS)
AM_V_CC = $(am__v_CC_$(V))
am__v_CC_ = $(am__v_CC_$(AM_DEFAULT_VERBOSITY))
am__v_CC_0 = @echo "  CC      " $@;
am__v_CC_1 = 
CCLD = $(CC)
LINK = $(LIBTOOL) $(AM_V_lt) --tag=CC $(AM_LIBTOOLFLAGS) \
	$(LIBTOOLFLAGS) --mode=link $(CCLD) $(AM_CFLAGS) $(CFLAGS) \
	$(AM_LDFLAGS) $(LDFLAGS) -o $@
AM_V_CCLD = $(am__v_CCLD_$(V))
am__v_CCLD_ = $(am__v_CCLD_$(AM_DEFAULT_VERBOSITY))
am__v_CCLD_0 = @echo "  CCLD    " $@;
am__v_CCLD_1 = 
SOURCES = $(libpd_la_SOURCES) $(libpdthrift_la_SOURCES)
DIST_SOURCES = $(libpd_la_SOURCES) $(libpdthrift_la_SOURCES)
am__can_run_installinfo = \
  case $$AM_UPDATE_INFO_DIR in \
    n|no|NO) false;; \
    *) (install-info --version) >/dev/null 2>&1;; \
  esac
am__py_compile = PYTHON=$(PYTHON) $(SHELL) $(py_compile)
am__pep3147_tweak = \
  sed -e 's|\.py$$||' -e 's|[^/]*$$|__pycache__/&.*.py|'
py_compile = $(top_srcdir)/py-compile
DATA = $(pddata_DATA)
HEADERS = $(pdinclude_HEADERS) $(pdthriftsrcinclude_HEADERS)
am__tagged_files = $(HEADERS) $(SOURCES) $(TAGS_FILES) $(LISP)
# Read a list of newline-separated strings from the standard input,
# and print each of them once, without duplicates.  Input order is
# *not* preserved.
am__uniquify_input = $(AWK) '\
  BEGIN { nonempty = 0; } \
  { items[$$0] = 1; nonempty = 1; } \
  END { if (nonempty) { for (i in items) print i; }; } \
'
# Make sure the list of sources is unique.  This is necessary because,
# e.g., the same source file might be shared among _SOURCES variables
# for different programs/libraries.
am__define_uniq_tagged_files = \
  list='$(am__tagged_files)'; \
  unique=`for i in $$list; do \
    if test -f "$$i"; then echo $$i; else echo $(srcdir)/$$i; fi; \
  done | $(am__uniquify_input)`
ETAGS = etags
CTAGS = ctags
DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)
ACLOCAL = ${SHELL} /root/bf-sde-8.2.0/switch-8.2.0/p4-build/missing aclocal-1.14
AMTAR = $${TAR-tar}
AM_DEFAULT_VERBOSITY = 1
AM_LDFLAGS = -L/root/bf-sde-8.2.0/install/lib
AR = ar
AUTOCONF = ${SHELL} /root/bf-sde-8.2.0/switch-8.2.0/p4-build/missing autoconf
AUTOHEADER = ${SHELL} /root/bf-sde-8.2.0/switch-8.2.0/p4-build/missing autoheader
AUTOMAKE = ${SHELL} /root/bf-sde-8.2.0/switch-8.2.0/p4-build/missing automake-1.14
AWK = mawk
BMV2TOFINO_HEADER_INCLUDE = 
BM_P4FLAGS = --field-aliases /root/bf-sde-8.2.0/switch-8.2.0/bmv2/field-aliases.bmv2 -I$(BMV2TOFINO_HEADER_INCLUDE)
BM_THRIFT_INCLUDE = 
CC = gcc -std=gnu99
CCDEPMODE = depmode=gcc3
CFLAGS = -g -O2
CPP = gcc -std=gnu99 -E
CPPFLAGS = 
CXX = g++
CXXCPP = g++ -E
CXXDEPMODE = depmode=gcc3
CXXFLAGS = -g -std=c++11
CYGPATH_W = echo
DEFS = -DHAVE_CONFIG_H
DEPDIR = .deps
DLLTOOL = false
DSYMUTIL = 
DUMPBIN = 
ECHO_C = 
ECHO_N = -n
ECHO_T = 
EGREP = /bin/grep -E
EXEEXT = 
FGREP = /bin/grep -F
FULL_INC_DIR = ${prefix}/include
GREP = /bin/grep
HAVE_CXX11 = 1
INSTALL = /usr/bin/install -c
INSTALL_DATA = ${INSTALL} -m 644
INSTALL_PROGRAM = ${INSTALL}
INSTALL_SCRIPT = ${INSTALL}
INSTALL_STRIP_PROGRAM = $(install_sh) -c -s
LD = /usr/bin/ld -m elf_x86_64
LDFLAGS = 
LIBOBJS = 
LIBS = 
LIBTOOL = $(SHELL) $(top_builddir)/libtool
LIPO = 
LN_S = ln -s
LTLIBOBJS = 
MAKEINFO = ${SHELL} /root/bf-sde-8.2.0/switch-8.2.0/p4-build/missing makeinfo
MANIFEST_TOOL = :
MKDIR_P = /bin/mkdir -p
MY_PYTHONPATH = /root/bf-sde-8.2.0/install/lib/python2.7/site-packages
NM = /usr/bin/nm -B
NMEDIT = 
OBJDUMP = objdump
OBJEXT = o
OTOOL = 
OTOOL64 = 
P4C = /root/bf-sde-8.2.0/install/bin/p4c-tofino
P4C_BM = 
P4C_BM_FLAGS = 
P4C_VERSION = 5.8.0 (7069cde)
P4C_VERSION_MAJOR = 5
P4FLAGS = 
P4JOBS = 4
P4PPFLAGS = 
P4_ARCHITECTURE = v1model
P4_BUILD_ASSUME_PDFIXED = 
P4_NAME = switch
P4_PATH = /root/bf-sde-8.2.0/switch-8.2.0/p4src/switch.p4
P4_PREFIX = dc
P4_VERSION = p4-14
PACKAGE = p4-build
PACKAGE_BUGREPORT = antonin@barefootnetworks.com
PACKAGE_NAME = p4-build
PACKAGE_STRING = p4-build 0.1
PACKAGE_TARNAME = p4-build
PACKAGE_URL = 
PACKAGE_VERSION = 0.1
PATH_SEPARATOR = :
PDCLI = /root/bf-sde-8.2.0/install/bin/gencli
PDFLAGS = 
PDGEN = /root/bf-sde-8.2.0/install/bin/generate_tofino_pd
PI_GEN_DEFS = 
PI_GEN_NATIVE_JSON = 
PI_GEN_PD = 
PYTHON = /usr/bin/python
PYTHON_EXEC_PREFIX = ${exec_prefix}
PYTHON_PLATFORM = linux2
PYTHON_PREFIX = ${prefix}
PYTHON_VERSION = 2.7
RANLIB = ranlib
SED = /bin/sed
SET_MAKE = 
SHELL = /bin/bash
STRIP = strip
THRIFT = 
THRIFT_LIB = 
VERSION = 0.1
abs_builddir = /root/bf-sde-8.2.0/switch-8.2.0/p4-build/bmv2-tofino
abs_srcdir = /root/bf-sde-8.2.0/switch-8.2.0/p4-build/bmv2-tofino
abs_top_builddir = /root/bf-sde-8.2.0/switch-8.2.0/p4-build
abs_top_srcdir = /root/bf-sde-8.2.0/switch-8.2.0/p4-build
ac_ct_AR = ar
ac_ct_CC = gcc
ac_ct_CXX = g++
ac_ct_DUMPBIN = 
am__include = include
am__leading_dot = .
am__quote = 
am__tar = $${TAR-tar} chof - "$$tardir"
am__untar = $${TAR-tar} xf -
bindir = ${exec_prefix}/bin
build = x86_64-unknown-linux-gnu
build_alias = 
build_cpu = x86_64
build_os = linux-gnu
build_vendor = unknown
builddir = .
datadir = ${datarootdir}
datarootdir = ${prefix}/share
docdir = ${datarootdir}/doc/${PACKAGE_TARNAME}
dvidir = ${docdir}
exec_prefix = ${prefix}
host = x86_64-unknown-linux-gnu
host_alias = 
host_cpu = x86_64
host_os = linux-gnu
host_vendor = unknown
htmldir = ${docdir}
includedir = ${prefix}/include
infodir = ${datarootdir}/info
install_sh = ${SHELL} /root/bf-sde-8.2.0/switch-8.2.0/p4-build/install-sh
libdir = ${exec_prefix}/lib
libexecdir = ${exec_prefix}/libexec
localedir = ${datarootdir}/locale
localstatedir = ${prefix}/var
mandir = ${datarootdir}/man
mkdir_p = $(MKDIR_P)
oldincludedir = /usr/include
pdfdir = ${docdir}
pkgpyexecdir = ${pyexecdir}/p4-build
pkgpythondir = ${pythondir}/p4-build
prefix = /root/bf-sde-8.2.0/install
program_transform_name = s,x,x,
psdir = ${docdir}
pyexecdir = ${exec_prefix}/lib/python2.7/site-packages
pythondir = ${prefix}/lib/python2.7/site-packages
sbindir = ${exec_prefix}/sbin
sharedstatedir = ${prefix}/com
srcdir = .
sysconfdir = ${prefix}/etc
target_alias = 
top_build_prefix = ../
top_builddir = ..
top_srcdir = ..
ACLOCAL_AMFLAGS = ${ACLOCAL_FLAGS} -I m4

#SUBDIRS = . tests
pd_files = \
switch/src/pd.cpp \
switch/src/pd_learning.cpp \
switch/src/pd_meters.cpp \
switch/src/pd_counters.cpp \
switch/src/pd_registers.cpp \
switch/src/pd_tables.cpp \
switch/src/pd_ageing.cpp \
switch/src/pd_client.h

pd_headers = \
switch/pd/pd.h \
switch/pd/pd_learning.h \
switch/pd/pd_meters.h \
switch/pd/pd_counters.h \
switch/pd/pd_registers.h \
switch/pd/pd_tables.h \
switch/pd/pd_types.h

pd_p4_IDL = switch/thrift/p4_pd_rpc.thrift
pd_p4_src = \
$(builddir)/switch/thrift-src/p4_pd_rpc_server.ipp \
$(builddir)/switch/thrift-src/pd_rpc_server.cpp \
$(builddir)/switch/thrift-src/pd_rpc_server.h

P4_JSON_OUTPUT = $(P4_NAME).json
P4_PI_NATIVE_JSON_OUTPUT = pi_native_$(P4_NAME).json
PRIMITIVES = $(srcdir)/primitives.json

# TODO(antonin):
# this is not portable (GNU make extension), but this is the only think I can
# think of right now to keep track of dependencies
# anyone with a better solution is welcome to change this
refdeps := $(shell ../tools/produce_deps.py /root/bf-sde-8.2.0/switch-8.2.0/p4src/switch.p4)
AM_P4PPFLAGS = $(P4PPFLAGS)
pd_p4_thrift_cxx = \
pd_thrift_gen/gen-cpp/p4_prefix0.cpp \
pd_thrift_gen/gen-cpp/p4_prefix1.cpp \
pd_thrift_gen/gen-cpp/p4_prefix2.cpp \
pd_thrift_gen/gen-cpp/p4_prefix.h \
pd_thrift_gen/gen-cpp/p4_pd_rpc_constants.cpp \
pd_thrift_gen/gen-cpp/p4_pd_rpc_constants.h \
pd_thrift_gen/gen-cpp/p4_pd_rpc_types.cpp \
pd_thrift_gen/gen-cpp/p4_pd_rpc_types.h

pd_p4_thrift_py = \
pd_thrift_gen/gen-py/p4_pd_rpc/constants.py \
pd_thrift_gen/gen-py/p4_pd_rpc/__init__.py \
pd_thrift_gen/gen-py/p4_pd_rpc/ttypes.py \
pd_thrift_gen/gen-py/p4_pd_rpc/dc.py \
pd_thrift_gen/gen-py/p4_pd_rpc/dc-remote

p4pythondir = ${prefix}/lib/python2.7/site-packages/tofinobmpd/switch/p4_pd_rpc
p4python_PYTHON = $(pd_p4_thrift_py)
extrapythondir = ${prefix}/lib/python2.7/site-packages/tofinobmpd/switch
extrapython_PYTHON = pd_thrift_gen/gen-py/__init__.py

# not necessary to install these files again, they come with pdfixed
pd_res_thrift_py = \
pd_thrift_gen/gen-py/res_pd_rpc/constants.py \
pd_thrift_gen/gen-py/res_pd_rpc/__init__.py \
pd_thrift_gen/gen-py/res_pd_rpc/ttypes.py

pd_thrift_files = $(pd_p4_thrift_cxx) $(pd_p4_thrift_py) $(pd_res_thrift_py)
BUILT_SOURCES = \
$(pd_files) \
$(pd_thrift_files)

pdincludedir = $(includedir)/tofinobmpd/switch/pd
pdinclude_HEADERS = $(pd_headers)
pdthriftsrcincludedir = $(includedir)/tofinobmpd/switch/thrift-src
pdthriftsrcinclude_HEADERS = switch/thrift-src/pd_rpc_server.h
pdlibdir = $(libdir)/tofinobmpd/switch
pdlib_LTLIBRARIES = libpd.la libpdthrift.la
libpd_la_SOURCES = \
$(pd_files)

libpdthrift_la_SOURCES = \
$(pd_thrift_files) \
switch/thrift-src/pd_rpc_server.cpp

AM_CPPFLAGS = -I$(includedir) -I$(builddir)/switch/ \
	-I$(builddir)/pd_thrift_gen/gen-cpp/ $(am__append_1)
pddatadir = $(datadir)/tofinobmpd/switch
pddata_DATA = $(P4_JSON_OUTPUT) $(am__append_2)
CLEANFILES = $(BUILT_SOURCES) pd_files.ts pd_thrift_files.ts \
	$(P4_JSON_OUTPUT) $(am__append_3)
all: $(BUILT_SOURCES)
	$(MAKE) $(AM_MAKEFLAGS) all-am

.SUFFIXES:
.SUFFIXES: .cpp .lo .o .obj
$(srcdir)/Makefile.in:  $(srcdir)/Makefile.am  $(am__configure_deps)
	@for dep in $?; do \
	  case '$(am__configure_deps)' in \
	    *$$dep*) \
	      ( cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh ) \
	        && { if test -f $@; then exit 0; else break; fi; }; \
	      exit 1;; \
	  esac; \
	done; \
	echo ' cd $(top_srcdir) && $(AUTOMAKE) --foreign bmv2-tofino/Makefile'; \
	$(am__cd) $(top_srcdir) && \
	  $(AUTOMAKE) --foreign bmv2-tofino/Makefile
.PRECIOUS: Makefile
Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status
	@case '$?' in \
	  *config.status*) \
	    cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh;; \
	  *) \
	    echo ' cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe)'; \
	    cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe);; \
	esac;

$(top_builddir)/config.status: $(top_srcdir)/configure $(CONFIG_STATUS_DEPENDENCIES)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh

$(top_srcdir)/configure:  $(am__configure_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(ACLOCAL_M4):  $(am__aclocal_m4_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(am__aclocal_m4_deps):

install-pdlibLTLIBRARIES: $(pdlib_LTLIBRARIES)
	@$(NORMAL_INSTALL)
	@list='$(pdlib_LTLIBRARIES)'; test -n "$(pdlibdir)" || list=; \
	list2=; for p in $$list; do \
	  if test -f $$p; then \
	    list2="$$list2 $$p"; \
	  else :; fi; \
	done; \
	test -z "$$list2" || { \
	  echo " $(MKDIR_P) '$(DESTDIR)$(pdlibdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(pdlibdir)" || exit 1; \
	  echo " $(LIBTOOL) $(AM_LIBTOOLFLAGS) $(LIBTOOLFLAGS) --mode=install $(INSTALL) $(INSTALL_STRIP_FLAG) $$list2 '$(DESTDIR)$(pdlibdir)'"; \
	  $(LIBTOOL) $(AM_LIBTOOLFLAGS) $(LIBTOOLFLAGS) --mode=install $(INSTALL) $(INSTALL_STRIP_FLAG) $$list2 "$(DESTDIR)$(pdlibdir)"; \
	}

uninstall-pdlibLTLIBRARIES:
	@$(NORMAL_UNINSTALL)
	@list='$(pdlib_LTLIBRARIES)'; test -n "$(pdlibdir)" || list=; \
	for p in $$list; do \
	  $(am__strip_dir) \
	  echo " $(LIBTOOL) $(AM_LIBTOOLFLAGS) $(LIBTOOLFLAGS) --mode=uninstall rm -f '$(DESTDIR)$(pdlibdir)/$$f'"; \
	  $(LIBTOOL) $(AM_LIBTOOLFLAGS) $(LIBTOOLFLAGS) --mode=uninstall rm -f "$(DESTDIR)$(pdlibdir)/$$f"; \
	done

clean-pdlibLTLIBRARIES:
	-test -z "$(pdlib_LTLIBRARIES)" || rm -f $(pdlib_LTLIBRARIES)
	@list='$(pdlib_LTLIBRARIES)'; \
	locs=`for p in $$list; do echo $$p; done | \
	      sed 's|^[^/]*$$|.|; s|/[^/]*$$||; s|$$|/so_locations|' | \
	      sort -u`; \
	test -z "$$locs" || { \
	  echo rm -f $${locs}; \
	  rm -f $${locs}; \
	}
switch/src/$(am__dirstamp):
	@$(MKDIR_P) switch/src
	@: > switch/src/$(am__dirstamp)
switch/src/$(DEPDIR)/$(am__dirstamp):
	@$(MKDIR_P) switch/src/$(DEPDIR)
	@: > switch/src/$(DEPDIR)/$(am__dirstamp)
switch/src/pd.lo: switch/src/$(am__dirstamp) \
	switch/src/$(DEPDIR)/$(am__dirstamp)
switch/src/pd_learning.lo: switch/src/$(am__dirstamp) \
	switch/src/$(DEPDIR)/$(am__dirstamp)
switch/src/pd_meters.lo: switch/src/$(am__dirstamp) \
	switch/src/$(DEPDIR)/$(am__dirstamp)
switch/src/pd_counters.lo: switch/src/$(am__dirstamp) \
	switch/src/$(DEPDIR)/$(am__dirstamp)
switch/src/pd_registers.lo: switch/src/$(am__dirstamp) \
	switch/src/$(DEPDIR)/$(am__dirstamp)
switch/src/pd_tables.lo: switch/src/$(am__dirstamp) \
	switch/src/$(DEPDIR)/$(am__dirstamp)
switch/src/pd_ageing.lo: switch/src/$(am__dirstamp) \
	switch/src/$(DEPDIR)/$(am__dirstamp)

libpd.la: $(libpd_la_OBJECTS) $(libpd_la_DEPENDENCIES) $(EXTRA_libpd_la_DEPENDENCIES) 
	$(AM_V_CXXLD)$(CXXLINK) -rpath $(pdlibdir) $(libpd_la_OBJECTS) $(libpd_la_LIBADD) $(LIBS)
pd_thrift_gen/gen-cpp/$(am__dirstamp):
	@$(MKDIR_P) pd_thrift_gen/gen-cpp
	@: > pd_thrift_gen/gen-cpp/$(am__dirstamp)
pd_thrift_gen/gen-cpp/$(DEPDIR)/$(am__dirstamp):
	@$(MKDIR_P) pd_thrift_gen/gen-cpp/$(DEPDIR)
	@: > pd_thrift_gen/gen-cpp/$(DEPDIR)/$(am__dirstamp)
pd_thrift_gen/gen-cpp/p4_prefix0.lo:  \
	pd_thrift_gen/gen-cpp/$(am__dirstamp) \
	pd_thrift_gen/gen-cpp/$(DEPDIR)/$(am__dirstamp)
pd_thrift_gen/gen-cpp/p4_prefix1.lo:  \
	pd_thrift_gen/gen-cpp/$(am__dirstamp) \
	pd_thrift_gen/gen-cpp/$(DEPDIR)/$(am__dirstamp)
pd_thrift_gen/gen-cpp/p4_prefix2.lo:  \
	pd_thrift_gen/gen-cpp/$(am__dirstamp) \
	pd_thrift_gen/gen-cpp/$(DEPDIR)/$(am__dirstamp)
pd_thrift_gen/gen-cpp/p4_pd_rpc_constants.lo:  \
	pd_thrift_gen/gen-cpp/$(am__dirstamp) \
	pd_thrift_gen/gen-cpp/$(DEPDIR)/$(am__dirstamp)
pd_thrift_gen/gen-cpp/p4_pd_rpc_types.lo:  \
	pd_thrift_gen/gen-cpp/$(am__dirstamp) \
	pd_thrift_gen/gen-cpp/$(DEPDIR)/$(am__dirstamp)
switch/thrift-src/$(am__dirstamp):
	@$(MKDIR_P) switch/thrift-src
	@: > switch/thrift-src/$(am__dirstamp)
switch/thrift-src/$(DEPDIR)/$(am__dirstamp):
	@$(MKDIR_P) switch/thrift-src/$(DEPDIR)
	@: > switch/thrift-src/$(DEPDIR)/$(am__dirstamp)
switch/thrift-src/pd_rpc_server.lo:  \
	switch/thrift-src/$(am__dirstamp) \
	switch/thrift-src/$(DEPDIR)/$(am__dirstamp)

libpdthrift.la: $(libpdthrift_la_OBJECTS) $(libpdthrift_la_DEPENDENCIES) $(EXTRA_libpdthrift_la_DEPENDENCIES) 
	$(AM_V_CXXLD)$(CXXLINK) -rpath $(pdlibdir) $(libpdthrift_la_OBJECTS) $(libpdthrift_la_LIBADD) $(LIBS)

mostlyclean-compile:
	-rm -f *.$(OBJEXT)
	-rm -f switch/src/*.$(OBJEXT)
	-rm -f switch/src/*.lo
	-rm -f switch/thrift-src/*.$(OBJEXT)
	-rm -f switch/thrift-src/*.lo
	-rm -f pd_thrift_gen/gen-cpp/*.$(OBJEXT)
	-rm -f pd_thrift_gen/gen-cpp/*.lo

distclean-compile:
	-rm -f *.tab.c

include switch/src/$(DEPDIR)/pd.Plo
include switch/src/$(DEPDIR)/pd_ageing.Plo
include switch/src/$(DEPDIR)/pd_counters.Plo
include switch/src/$(DEPDIR)/pd_learning.Plo
include switch/src/$(DEPDIR)/pd_meters.Plo
include switch/src/$(DEPDIR)/pd_registers.Plo
include switch/src/$(DEPDIR)/pd_tables.Plo
include switch/thrift-src/$(DEPDIR)/pd_rpc_server.Plo
include pd_thrift_gen/gen-cpp/$(DEPDIR)/p4_pd_rpc_constants.Plo
include pd_thrift_gen/gen-cpp/$(DEPDIR)/p4_pd_rpc_types.Plo
include pd_thrift_gen/gen-cpp/$(DEPDIR)/p4_prefix0.Plo
include pd_thrift_gen/gen-cpp/$(DEPDIR)/p4_prefix1.Plo
include pd_thrift_gen/gen-cpp/$(DEPDIR)/p4_prefix2.Plo

.cpp.o:
	$(AM_V_CXX)depbase=`echo $@ | sed 's|[^/]*$$|$(DEPDIR)/&|;s|\.o$$||'`;\
	$(CXXCOMPILE) -MT $@ -MD -MP -MF $$depbase.Tpo -c -o $@ $< &&\
	$(am__mv) $$depbase.Tpo $$depbase.Po
#	$(AM_V_CXX)source='$<' object='$@' libtool=no \
#	DEPDIR=$(DEPDIR) $(CXXDEPMODE) $(depcomp) \
#	$(AM_V_CXX_no)$(CXXCOMPILE) -c -o $@ $<

.cpp.obj:
	$(AM_V_CXX)depbase=`echo $@ | sed 's|[^/]*$$|$(DEPDIR)/&|;s|\.obj$$||'`;\
	$(CXXCOMPILE) -MT $@ -MD -MP -MF $$depbase.Tpo -c -o $@ `$(CYGPATH_W) '$<'` &&\
	$(am__mv) $$depbase.Tpo $$depbase.Po
#	$(AM_V_CXX)source='$<' object='$@' libtool=no \
#	DEPDIR=$(DEPDIR) $(CXXDEPMODE) $(depcomp) \
#	$(AM_V_CXX_no)$(CXXCOMPILE) -c -o $@ `$(CYGPATH_W) '$<'`

.cpp.lo:
	$(AM_V_CXX)depbase=`echo $@ | sed 's|[^/]*$$|$(DEPDIR)/&|;s|\.lo$$||'`;\
	$(LTCXXCOMPILE) -MT $@ -MD -MP -MF $$depbase.Tpo -c -o $@ $< &&\
	$(am__mv) $$depbase.Tpo $$depbase.Plo
#	$(AM_V_CXX)source='$<' object='$@' libtool=yes \
#	DEPDIR=$(DEPDIR) $(CXXDEPMODE) $(depcomp) \
#	$(AM_V_CXX_no)$(LTCXXCOMPILE) -c -o $@ $<

mostlyclean-libtool:
	-rm -f *.lo

clean-libtool:
	-rm -rf .libs _libs
	-rm -rf switch/src/.libs switch/src/_libs
	-rm -rf switch/thrift-src/.libs switch/thrift-src/_libs
	-rm -rf pd_thrift_gen/gen-cpp/.libs pd_thrift_gen/gen-cpp/_libs
install-extrapythonPYTHON: $(extrapython_PYTHON)
	@$(NORMAL_INSTALL)
	@list='$(extrapython_PYTHON)'; dlist=; list2=; test -n "$(extrapythondir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(extrapythondir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(extrapythondir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then b=; else b="$(srcdir)/"; fi; \
	  if test -f $$b$$p; then \
	    $(am__strip_dir) \
	    dlist="$$dlist $$f"; \
	    list2="$$list2 $$b$$p"; \
	  else :; fi; \
	done; \
	for file in $$list2; do echo $$file; done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(extrapythondir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(extrapythondir)" || exit $$?; \
	done || exit $$?; \
	if test -n "$$dlist"; then \
	  $(am__py_compile) --destdir "$(DESTDIR)" \
	                    --basedir "$(extrapythondir)" $$dlist; \
	else :; fi

uninstall-extrapythonPYTHON:
	@$(NORMAL_UNINSTALL)
	@list='$(extrapython_PYTHON)'; test -n "$(extrapythondir)" || list=; \
	py_files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$py_files" || exit 0; \
	dir='$(DESTDIR)$(extrapythondir)'; \
	pyc_files=`echo "$$py_files" | sed 's|$$|c|'`; \
	pyo_files=`echo "$$py_files" | sed 's|$$|o|'`; \
	py_files_pep3147=`echo "$$py_files" | $(am__pep3147_tweak)`; \
	echo "$$py_files_pep3147";\
	pyc_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|c|'`; \
	pyo_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|o|'`; \
	st=0; \
	for files in \
	  "$$py_files" \
	  "$$pyc_files" \
	  "$$pyo_files" \
	  "$$pyc_files_pep3147" \
	  "$$pyo_files_pep3147" \
	; do \
	  $(am__uninstall_files_from_dir) || st=$$?; \
	done; \
	exit $$st
install-p4pythonPYTHON: $(p4python_PYTHON)
	@$(NORMAL_INSTALL)
	@list='$(p4python_PYTHON)'; dlist=; list2=; test -n "$(p4pythondir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(p4pythondir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(p4pythondir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then b=; else b="$(srcdir)/"; fi; \
	  if test -f $$b$$p; then \
	    $(am__strip_dir) \
	    dlist="$$dlist $$f"; \
	    list2="$$list2 $$b$$p"; \
	  else :; fi; \
	done; \
	for file in $$list2; do echo $$file; done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(p4pythondir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(p4pythondir)" || exit $$?; \
	done || exit $$?; \
	if test -n "$$dlist"; then \
	  $(am__py_compile) --destdir "$(DESTDIR)" \
	                    --basedir "$(p4pythondir)" $$dlist; \
	else :; fi

uninstall-p4pythonPYTHON:
	@$(NORMAL_UNINSTALL)
	@list='$(p4python_PYTHON)'; test -n "$(p4pythondir)" || list=; \
	py_files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$py_files" || exit 0; \
	dir='$(DESTDIR)$(p4pythondir)'; \
	pyc_files=`echo "$$py_files" | sed 's|$$|c|'`; \
	pyo_files=`echo "$$py_files" | sed 's|$$|o|'`; \
	py_files_pep3147=`echo "$$py_files" | $(am__pep3147_tweak)`; \
	echo "$$py_files_pep3147";\
	pyc_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|c|'`; \
	pyo_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|o|'`; \
	st=0; \
	for files in \
	  "$$py_files" \
	  "$$pyc_files" \
	  "$$pyo_files" \
	  "$$pyc_files_pep3147" \
	  "$$pyo_files_pep3147" \
	; do \
	  $(am__uninstall_files_from_dir) || st=$$?; \
	done; \
	exit $$st
install-pddataDATA: $(pddata_DATA)
	@$(NORMAL_INSTALL)
	@list='$(pddata_DATA)'; test -n "$(pddatadir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(pddatadir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(pddatadir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(pddatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(pddatadir)" || exit $$?; \
	done

uninstall-pddataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(pddata_DATA)'; test -n "$(pddatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(pddatadir)'; $(am__uninstall_files_from_dir)
install-pdincludeHEADERS: $(pdinclude_HEADERS)
	@$(NORMAL_INSTALL)
	@list='$(pdinclude_HEADERS)'; test -n "$(pdincludedir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(pdincludedir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(pdincludedir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_HEADER) $$files '$(DESTDIR)$(pdincludedir)'"; \
	  $(INSTALL_HEADER) $$files "$(DESTDIR)$(pdincludedir)" || exit $$?; \
	done

uninstall-pdincludeHEADERS:
	@$(NORMAL_UNINSTALL)
	@list='$(pdinclude_HEADERS)'; test -n "$(pdincludedir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(pdincludedir)'; $(am__uninstall_files_from_dir)
install-pdthriftsrcincludeHEADERS: $(pdthriftsrcinclude_HEADERS)
	@$(NORMAL_INSTALL)
	@list='$(pdthriftsrcinclude_HEADERS)'; test -n "$(pdthriftsrcincludedir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(pdthriftsrcincludedir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(pdthriftsrcincludedir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_HEADER) $$files '$(DESTDIR)$(pdthriftsrcincludedir)'"; \
	  $(INSTALL_HEADER) $$files "$(DESTDIR)$(pdthriftsrcincludedir)" || exit $$?; \
	done

uninstall-pdthriftsrcincludeHEADERS:
	@$(NORMAL_UNINSTALL)
	@list='$(pdthriftsrcinclude_HEADERS)'; test -n "$(pdthriftsrcincludedir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(pdthriftsrcincludedir)'; $(am__uninstall_files_from_dir)

ID: $(am__tagged_files)
	$(am__define_uniq_tagged_files); mkid -fID $$unique
tags: tags-am
TAGS: tags

tags-am: $(TAGS_DEPENDENCIES) $(am__tagged_files)
	set x; \
	here=`pwd`; \
	$(am__define_uniq_tagged_files); \
	shift; \
	if test -z "$(ETAGS_ARGS)$$*$$unique"; then :; else \
	  test -n "$$unique" || unique=$$empty_fix; \
	  if test $$# -gt 0; then \
	    $(ETAGS) $(ETAGSFLAGS) $(AM_ETAGSFLAGS) $(ETAGS_ARGS) \
	      "$$@" $$unique; \
	  else \
	    $(ETAGS) $(ETAGSFLAGS) $(AM_ETAGSFLAGS) $(ETAGS_ARGS) \
	      $$unique; \
	  fi; \
	fi
ctags: ctags-am

CTAGS: ctags
ctags-am: $(TAGS_DEPENDENCIES) $(am__tagged_files)
	$(am__define_uniq_tagged_files); \
	test -z "$(CTAGS_ARGS)$$unique" \
	  || $(CTAGS) $(CTAGSFLAGS) $(AM_CTAGSFLAGS) $(CTAGS_ARGS) \
	     $$unique

GTAGS:
	here=`$(am__cd) $(top_builddir) && pwd` \
	  && $(am__cd) $(top_srcdir) \
	  && gtags -i $(GTAGS_ARGS) "$$here"
cscopelist: cscopelist-am

cscopelist-am: $(am__tagged_files)
	list='$(am__tagged_files)'; \
	case "$(srcdir)" in \
	  [\\/]* | ?:[\\/]*) sdir="$(srcdir)" ;; \
	  *) sdir=$(subdir)/$(srcdir) ;; \
	esac; \
	for i in $$list; do \
	  if test -f "$$i"; then \
	    echo "$(subdir)/$$i"; \
	  else \
	    echo "$$sdir/$$i"; \
	  fi; \
	done >> $(top_builddir)/cscope.files

distclean-tags:
	-rm -f TAGS ID GTAGS GRTAGS GSYMS GPATH tags

distdir: $(DISTFILES)
	@srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	topsrcdirstrip=`echo "$(top_srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	list='$(DISTFILES)'; \
	  dist_files=`for file in $$list; do echo $$file; done | \
	  sed -e "s|^$$srcdirstrip/||;t" \
	      -e "s|^$$topsrcdirstrip/|$(top_builddir)/|;t"`; \
	case $$dist_files in \
	  */*) $(MKDIR_P) `echo "$$dist_files" | \
			   sed '/\//!d;s|^|$(distdir)/|;s,/[^/]*$$,,' | \
			   sort -u` ;; \
	esac; \
	for file in $$dist_files; do \
	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
	  if test -d $$d/$$file; then \
	    dir=`echo "/$$file" | sed -e 's,/[^/]*$$,,'`; \
	    if test -d "$(distdir)/$$file"; then \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    if test -d $(srcdir)/$$file && test $$d != $(srcdir); then \
	      cp -fpR $(srcdir)/$$file "$(distdir)$$dir" || exit 1; \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    cp -fpR $$d/$$file "$(distdir)$$dir" || exit 1; \
	  else \
	    test -f "$(distdir)/$$file" \
	    || cp -p $$d/$$file "$(distdir)/$$file" \
	    || exit 1; \
	  fi; \
	done
check-am: all-am
check: $(BUILT_SOURCES)
	$(MAKE) $(AM_MAKEFLAGS) check-am
all-am: Makefile $(LTLIBRARIES) $(DATA) $(HEADERS)
installdirs:
	for dir in "$(DESTDIR)$(pdlibdir)" "$(DESTDIR)$(extrapythondir)" "$(DESTDIR)$(p4pythondir)" "$(DESTDIR)$(pddatadir)" "$(DESTDIR)$(pdincludedir)" "$(DESTDIR)$(pdthriftsrcincludedir)"; do \
	  test -z "$$dir" || $(MKDIR_P) "$$dir"; \
	done
install: $(BUILT_SOURCES)
	$(MAKE) $(AM_MAKEFLAGS) install-am
install-exec: install-exec-am
install-data: install-data-am
uninstall: uninstall-am

install-am: all-am
	@$(MAKE) $(AM_MAKEFLAGS) install-exec-am install-data-am

installcheck: installcheck-am
install-strip:
	if test -z '$(STRIP)'; then \
	  $(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	    install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	      install; \
	else \
	  $(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	    install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	    "INSTALL_PROGRAM_ENV=STRIPPROG='$(STRIP)'" install; \
	fi
mostlyclean-generic:

clean-generic:
	-test -z "$(CLEANFILES)" || rm -f $(CLEANFILES)

distclean-generic:
	-test -z "$(CONFIG_CLEAN_FILES)" || rm -f $(CONFIG_CLEAN_FILES)
	-test . = "$(srcdir)" || test -z "$(CONFIG_CLEAN_VPATH_FILES)" || rm -f $(CONFIG_CLEAN_VPATH_FILES)
	-rm -f switch/src/$(DEPDIR)/$(am__dirstamp)
	-rm -f switch/src/$(am__dirstamp)
	-rm -f switch/thrift-src/$(DEPDIR)/$(am__dirstamp)
	-rm -f switch/thrift-src/$(am__dirstamp)
	-rm -f pd_thrift_gen/gen-cpp/$(DEPDIR)/$(am__dirstamp)
	-rm -f pd_thrift_gen/gen-cpp/$(am__dirstamp)

maintainer-clean-generic:
	@echo "This command is intended for maintainers to use"
	@echo "it deletes files that may require special tools to rebuild."
	-test -z "$(BUILT_SOURCES)" || rm -f $(BUILT_SOURCES)
clean: clean-am

clean-am: clean-generic clean-libtool clean-pdlibLTLIBRARIES \
	mostlyclean-am

distclean: distclean-am
	-rm -rf switch/src/$(DEPDIR) switch/thrift-src/$(DEPDIR) pd_thrift_gen/gen-cpp/$(DEPDIR)
	-rm -f Makefile
distclean-am: clean-am distclean-compile distclean-generic \
	distclean-tags

dvi: dvi-am

dvi-am:

html: html-am

html-am:

info: info-am

info-am:

install-data-am: install-extrapythonPYTHON install-p4pythonPYTHON \
	install-pddataDATA install-pdincludeHEADERS \
	install-pdlibLTLIBRARIES install-pdthriftsrcincludeHEADERS

install-dvi: install-dvi-am

install-dvi-am:

install-exec-am:

install-html: install-html-am

install-html-am:

install-info: install-info-am

install-info-am:

install-man:

install-pdf: install-pdf-am

install-pdf-am:

install-ps: install-ps-am

install-ps-am:

installcheck-am:

maintainer-clean: maintainer-clean-am
	-rm -rf switch/src/$(DEPDIR) switch/thrift-src/$(DEPDIR) pd_thrift_gen/gen-cpp/$(DEPDIR)
	-rm -f Makefile
maintainer-clean-am: distclean-am maintainer-clean-generic

mostlyclean: mostlyclean-am

mostlyclean-am: mostlyclean-compile mostlyclean-generic \
	mostlyclean-libtool

pdf: pdf-am

pdf-am:

ps: ps-am

ps-am:

uninstall-am: uninstall-extrapythonPYTHON uninstall-p4pythonPYTHON \
	uninstall-pddataDATA uninstall-pdincludeHEADERS \
	uninstall-pdlibLTLIBRARIES uninstall-pdthriftsrcincludeHEADERS

.MAKE: all check install install-am install-strip

.PHONY: CTAGS GTAGS TAGS all all-am check check-am clean clean-generic \
	clean-libtool clean-pdlibLTLIBRARIES cscopelist-am ctags \
	ctags-am distclean distclean-compile distclean-generic \
	distclean-libtool distclean-tags distdir dvi dvi-am html \
	html-am info info-am install install-am install-data \
	install-data-am install-dvi install-dvi-am install-exec \
	install-exec-am install-extrapythonPYTHON install-html \
	install-html-am install-info install-info-am install-man \
	install-p4pythonPYTHON install-pddataDATA install-pdf \
	install-pdf-am install-pdincludeHEADERS \
	install-pdlibLTLIBRARIES install-pdthriftsrcincludeHEADERS \
	install-ps install-ps-am install-strip installcheck \
	installcheck-am installdirs maintainer-clean \
	maintainer-clean-generic mostlyclean mostlyclean-compile \
	mostlyclean-generic mostlyclean-libtool pdf pdf-am ps ps-am \
	tags tags-am uninstall uninstall-am \
	uninstall-extrapythonPYTHON uninstall-p4pythonPYTHON \
	uninstall-pddataDATA uninstall-pdincludeHEADERS \
	uninstall-pdlibLTLIBRARIES uninstall-pdthriftsrcincludeHEADERS


pd_files.ts : $(P4_PATH) $(refdeps)
	@rm -f pd_files.tmp
	@touch pd_files.tmp
	PYTHONPATH=$(MY_PYTHONPATH):$$PYTHONPATH $(P4C_BM) --pd $(builddir)/$(P4_NAME)/ --p4-prefix $(P4_PREFIX) \
	$(P4C_BM_FLAGS) --json $(builddir)/$(P4_JSON_OUTPUT) --primitives $(PRIMITIVES) \
	$(BM_P4FLAGS) $(AM_P4PPFLAGS) $(P4_PATH) -D__TARGET_TOFINO__ -DBMV2TOFINO
#	@$(PI_GEN_NATIVE_JSON) $(builddir)/$(P4_JSON_OUTPUT) > $(builddir)/$(P4_PI_NATIVE_JSON_OUTPUT)
#	@$(PI_GEN_DEFS) -c $(builddir)/$(P4_JSON_OUTPUT)  -d $(top_builddir)/ -n $(P4_PREFIX)
	@mv -f pd_files.tmp $@
$(pd_files) $(pd_p4_IDL) $(pd_p4_src) : pd_files.ts
	@if test -f $@; then :; else \
	  trap 'rm -rf pd_files.lock pd_files.ts' 1 2 13 15; \
	if mkdir pd_files.lock 2>/dev/null; then \
	  rm -f pd_files.ts; \
	  $(MAKE) $(AM_MAKEFLAGS) pd_files.ts; \
	  result=$$?; rm -rf pd_files.lock; exit $$result; \
	else \
	  while test -d pd_files.lock; do sleep 1; done; \
	    test -f pd_files.ts; \
	  fi; \
	fi

pd_thrift_files.ts: $(pd_files) $(pd_p4_IDL) $(pd_res_IDL)
	@rm -f pd_thrift_files.tmp
	@touch pd_thrift_files.tmp
	$(THRIFT) -o $(builddir)/pd_thrift_gen/ -I $(BM_THRIFT_INCLUDE) --gen cpp -r $(pd_p4_IDL)
	$(THRIFT) -o $(builddir)/pd_thrift_gen/ -I $(BM_THRIFT_INCLUDE) --gen py -r $(pd_p4_IDL)
	mv -f pd_thrift_gen/gen-cpp/$(P4_PREFIX).h pd_thrift_gen/gen-cpp/p4_prefix.h
	sed --in-place 's/include "$(P4_PREFIX).h"/include "p4_prefix.h"/' pd_thrift_gen/gen-cpp/$(P4_PREFIX).cpp
	$(PYTHON) $(top_srcdir)/tools/split_pd_thrift.py pd_thrift_gen/gen-cpp/$(P4_PREFIX).cpp pd_thrift_gen/gen-cpp/ 3
	@mv -f pd_thrift_files.tmp $@
$(pd_thrift_files): pd_thrift_files.ts
	@if test -f $@; then :; else \
	  trap 'rm -rf pd_thrift_files.lock pd_thrift_files.ts' 1 2 13 15; \
	if mkdir pd_thrift_files.lock 2>/dev/null; then \
	  rm -f pd_thrift_files.ts; \
	  $(MAKE) $(AM_MAKEFLAGS) pd_thrift_files.ts; \
	  result=$$?; rm -rf pd_thrift_files.lock; exit $$result; \
	else \
	  while test -d pd_thrift_files.lock; do sleep 1; done; \
	    test -f pd_thrift_files.ts; \
	  fi; \
	fi

# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
